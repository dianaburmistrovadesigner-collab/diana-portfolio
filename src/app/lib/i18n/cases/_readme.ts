/**
 * Case study i18n — pattern for Manus
 *
 * 1. One file per case: amigochat.ts, aimlapi.ts, overchat.ts, strevolut.ts
 * 2. Export getCaseXxxContent(locale) returning typed strings
 * 3. Case page imports hook + replaces hardcoded EN strings
 *
 * Reference copy: content-export/CASE-*.md
 * Do NOT create separate RU page components.
 */

export {};
